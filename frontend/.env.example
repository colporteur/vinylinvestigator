# The Cloudflare Worker URL printed by `wrangler deploy`.
# Anything prefixed PUBLIC_ is exposed to the browser; the Worker URL is not secret.
PUBLIC_WORKER_URL=https://vinyl-investigator-worker.YOUR-SUBDOMAIN.workers.dev
